name: my-first23adsasd
on:
  push:
    branches:
       - main
workflow-dispatch:
inputs:
  name: "type your name"
jobs:
    greetings:
      runs-on: ubuntu-latest
      steps:
      - name: Print data
        run: echo "Input guy:"
     
